# Changelog

## 0.1.0

- Added structured `.rommy` execution logs for commands and scripts.
- Added atomic writes and file locking for safer concurrent output handling.
- Added `validate` subcommand with text and JSON output modes.
- Added `show` subcommand with text and JSON record output.
- Added CI checks for formatting, clippy, and tests.
